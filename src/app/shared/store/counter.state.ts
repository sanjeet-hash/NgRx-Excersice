import { counterModel } from "./counter.model";//here we import the iterface of datatypes.

export const initialState:counterModel={ //here we defined the initial state.
    counter:0,
    chanelName:'Sanju Techies'
}